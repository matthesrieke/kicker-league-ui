import { PlayerPartyPipe } from './player-party.pipe';

describe('PlayerPartyPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerPartyPipe();
    expect(pipe).toBeTruthy();
  });
});
